
export interface IItemCardProps {
    /**
     * @description send image url to display to the card.
     */
    image: string

    /**
     * @description defines if the card is hoverable.
     * @default true
     */
    hoverable?: boolean
}